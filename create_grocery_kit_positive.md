# API Test: Create a Custom Grocery Kit (Positive Scenario)

**Endpoint:** `POST /api/v1/kits`  
**Description:** Validates that a user can successfully create a new customized product kit by sending a valid string name.

### 📤 Request Body (JSON)
```json
{
    "name": "Healthy Breakfast Pack"
}
